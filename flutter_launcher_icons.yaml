flutter_launcher_icons:
  ios: true
  android: true

  # Use the same new design for both platforms
  image_path: assets/icon/MB-Icon.png

  # Optional Android adaptive icons (they will use the same base design)
  adaptive_icon_foreground: assets/icon/MB-Icon.png
  adaptive_icon_background: "#0C1413"
